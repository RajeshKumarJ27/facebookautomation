1. First you have to install python and any IDE or Text Editor
2. Open command prompt and type pip3 install selenium(library)
3. Selenium is one of the library function to work with webdriver
4. Without opeing any browser manually we can automatically login through 
   code